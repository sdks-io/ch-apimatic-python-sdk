"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.assigned_role import (
    AssignedRole,
)


class Member(object):
    """Implementation of the 'Member' model.

    Attributes:
        user_id (str): Unique user ID. If a user is a member in multiple
            organizations this ID will stay the same.
        name (str): Name of the member as set a personal user profile.
        email (str): Email of the member as set in personal user profile.
        role (Role1): DEPRECATED. Use `assignedRoles` instead. Role of the member in
            the organization. For organizations that have migrated to custom roles,
            this field is frozen at the pre-migration value and does not reflect
            current role assignments.
        joined_at (datetime): Timestamp the member joined the organization. ISO-8601.
        assigned_roles (List[AssignedRole]): Custom roles and System roles assigned
            to this member
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "user_id": "userId",
        "name": "name",
        "email": "email",
        "role": "role",
        "joined_at": "joinedAt",
        "assigned_roles": "assignedRoles",
    }

    _optionals = [
        "user_id",
        "name",
        "email",
        "role",
        "joined_at",
        "assigned_roles",
    ]

    def __init__(
        self,
        user_id=APIHelper.SKIP,
        name=APIHelper.SKIP,
        email=APIHelper.SKIP,
        role=APIHelper.SKIP,
        joined_at=APIHelper.SKIP,
        assigned_roles=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a Member instance."""
        # Initialize members of the class
        if user_id is not APIHelper.SKIP:
            self.user_id = user_id
        if name is not APIHelper.SKIP:
            self.name = name
        if email is not APIHelper.SKIP:
            self.email = email
        if role is not APIHelper.SKIP:
            self.role = role
        if joined_at is not APIHelper.SKIP:
            self.joined_at =\
                 APIHelper.apply_datetime_converter(
                joined_at, APIHelper.RFC3339DateTime)\
                 if joined_at else None
        if assigned_roles is not APIHelper.SKIP:
            self.assigned_roles = assigned_roles

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        user_id =\
            dictionary.get("userId")\
            if dictionary.get("userId")\
                else APIHelper.SKIP
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else APIHelper.SKIP
        email =\
            dictionary.get("email")\
            if dictionary.get("email")\
                else APIHelper.SKIP
        role =\
            dictionary.get("role")\
            if dictionary.get("role")\
                else APIHelper.SKIP
        joined_at = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("joinedAt")).datetime\
            if dictionary.get("joinedAt") else APIHelper.SKIP
        assigned_roles = None
        if dictionary.get("assignedRoles") is not None:
            assigned_roles = [
                AssignedRole.from_dictionary(x)
                    for x in dictionary.get("assignedRoles")
            ]
        else:
            assigned_roles = APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(user_id,
                   name,
                   email,
                   role,
                   joined_at,
                   assigned_roles,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _user_id=(
            self.user_id
            if hasattr(self, "user_id")
            else None
        )
        _name=(
            self.name
            if hasattr(self, "name")
            else None
        )
        _email=(
            self.email
            if hasattr(self, "email")
            else None
        )
        _role=(
            self.role
            if hasattr(self, "role")
            else None
        )
        _joined_at=(
            self.joined_at
            if hasattr(self, "joined_at")
            else None
        )
        _assigned_roles=(
            self.assigned_roles
            if hasattr(self, "assigned_roles")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"user_id={_user_id!r}, "
            f"name={_name!r}, "
            f"email={_email!r}, "
            f"role={_role!r}, "
            f"joined_at={_joined_at!r}, "
            f"assigned_roles={_assigned_roles!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _user_id=(
            self.user_id
            if hasattr(self, "user_id")
            else None
        )
        _name=(
            self.name
            if hasattr(self, "name")
            else None
        )
        _email=(
            self.email
            if hasattr(self, "email")
            else None
        )
        _role=(
            self.role
            if hasattr(self, "role")
            else None
        )
        _joined_at=(
            self.joined_at
            if hasattr(self, "joined_at")
            else None
        )
        _assigned_roles=(
            self.assigned_roles
            if hasattr(self, "assigned_roles")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"user_id={_user_id!s}, "
            f"name={_name!s}, "
            f"email={_email!s}, "
            f"role={_role!s}, "
            f"joined_at={_joined_at!s}, "
            f"assigned_roles={_assigned_roles!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
