"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.click_stack_on_click_filter_template import (
    ClickStackOnClickFilterTemplate,
)


class ClickStackOnClickDashboard(object):
    """Implementation of the 'ClickStackOnClickDashboard' model.

    Attributes:
        mtype (str): OnClick variant discriminator. Must be "dashboard" for dashboard
            link-outs.
        target (ClickStackOnClickTargetIdVariant |
            ClickStackOnClickTargetTemplateVariant): The model property of type
            ClickStackOnClickTargetIdVariant | ClickStackOnClickTargetTemplateVariant.
        where_template (str): Optional WHERE clause template applied to the
            destination dashboard.
        where_language (WhereLanguage5): Language of the rendered whereTemplate.
        filters (List[ClickStackOnClickFilterTemplate]): Optional dashboard filter
            templates rendered against the clicked row.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mtype": "type",
        "target": "target",
        "where_template": "whereTemplate",
        "where_language": "whereLanguage",
        "filters": "filters",
    }

    _optionals = [
        "where_template",
        "where_language",
        "filters",
    ]

    def __init__(
        self,
        target=None,
        where_template=APIHelper.SKIP,
        where_language=APIHelper.SKIP,
        filters=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickStackOnClickDashboard instance."""
        # Initialize members of the class
        self.mtype = "dashboard"
        self.target = target
        if where_template is not APIHelper.SKIP:
            self.where_template = where_template
        if where_language is not APIHelper.SKIP:
            self.where_language = where_language
        if filters is not APIHelper.SKIP:
            self.filters = filters

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from openapispecforclickhousecloud.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        target = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("ClickStackOnClickTarget"),
            dictionary.get("target"),
            False)\
            if dictionary.get("target") is not None\
            else None
        where_template =\
            dictionary.get("whereTemplate")\
            if dictionary.get("whereTemplate")\
                else APIHelper.SKIP
        where_language =\
            dictionary.get("whereLanguage")\
            if dictionary.get("whereLanguage")\
                else APIHelper.SKIP
        filters = None
        if dictionary.get("filters") is not None:
            filters = [
                ClickStackOnClickFilterTemplate.from_dictionary(x)
                    for x in dictionary.get("filters")
            ]
        else:
            filters = APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(target,
                   where_template,
                   where_language,
                   filters,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        from openapispecforclickhousecloud.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.mtype,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and (UnionTypeLookUp.get("ClickStackOnClickTarget")
                .validate(dictionary.target).is_valid)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("type"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and (UnionTypeLookUp.get("ClickStackOnClickTarget")
            .validate(dictionary.get("target")).is_valid)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _mtype=self.mtype
        _target=self.target
        _where_template=(
            self.where_template
            if hasattr(self, "where_template")
            else None
        )
        _where_language=(
            self.where_language
            if hasattr(self, "where_language")
            else None
        )
        _filters=(
            self.filters
            if hasattr(self, "filters")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!r}, "
            f"target={_target!r}, "
            f"where_template={_where_template!r}, "
            f"where_language={_where_language!r}, "
            f"filters={_filters!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _mtype=self.mtype
        _target=self.target
        _where_template=(
            self.where_template
            if hasattr(self, "where_template")
            else None
        )
        _where_language=(
            self.where_language
            if hasattr(self, "where_language")
            else None
        )
        _filters=(
            self.filters
            if hasattr(self, "filters")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!s}, "
            f"target={_target!s}, "
            f"where_template={_where_template!s}, "
            f"where_language={_where_language!s}, "
            f"filters={_filters!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
