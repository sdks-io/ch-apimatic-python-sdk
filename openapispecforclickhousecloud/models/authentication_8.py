"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501

class Authentication8(object):
    """Implementation of the 'Authentication8' enum.

    Authentication method. IAM_USER is for S3, GCS, and DigitalOcean Spaces. IAM_ROLE
    is for S3 only. SERVICE_ACCOUNT is for GCS only. For GCS,
    SERVICE_ACCOUNT_WORKLOAD_IDENTITY is in Private Preview. ClickPipes uses the GCP
    service account returned in gcpWorkloadIdentity.principal by the operation with
    operationId clickPipesServiceContextGet; grant it access to the source resources.
    CONNECTION_STRING is for Azure Blob Storage. PUBLIC uses no authentication.

    Attributes:
        IAM_ROLE: The enum member of type str.
        IAM_USER: The enum member of type str.
        CONNECTION_STRING: The enum member of type str.
        SERVICE_ACCOUNT: The enum member of type str.
        SERVICE_ACCOUNT_WORKLOAD_IDENTITY: The enum member of type str.

    """

    IAM_ROLE = "IAM_ROLE"

    IAM_USER = "IAM_USER"

    CONNECTION_STRING = "CONNECTION_STRING"

    SERVICE_ACCOUNT = "SERVICE_ACCOUNT"

    SERVICE_ACCOUNT_WORKLOAD_IDENTITY = "SERVICE_ACCOUNT_WORKLOAD_IDENTITY"

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
