"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.click_stack_background_chart import (
    ClickStackBackgroundChart,
)
from openapispecforclickhousecloud.models.click_stack_formula import (
    ClickStackFormula,
)
from openapispecforclickhousecloud.models.click_stack_number_format import (
    ClickStackNumberFormat,
)
from openapispecforclickhousecloud.models.click_stack_select_item import (
    ClickStackSelectItem,
)


class ClickStackNumberBuilderChartConfig(object):
    """Implementation of the 'ClickStackNumberBuilderChartConfig' model.

    Attributes:
        display_type (str): Display type discriminator. Must be "number" for single
            big-number charts.
        source_id (str): ID of the data source to query.
        select (List[ClickStackSelectItem]): Exactly one aggregated value to display
            as a single number — unless "formulas" is set, in which case the select
            items are the formula's operands and the (single) formula value is
            displayed instead.
        formulas (List[ClickStackFormula]): A single derived value computed from the
            select items via letter-ref arithmetic ("A" = select[0], "B" = select[1],
            ...). Metric, log, and trace sources only. Number tiles display the
            formula value and always hide the operand series.
        number_format (ClickStackNumberFormat): The model property of type
            ClickStackNumberFormat.
        color (Color4): Optional static color applied to the displayed number.
        color_rules (List[ClickStackNumericColorCondition |
            ClickStackBetweenColorCondition | ClickStackEqualityColorCondition] |
            None): Ordered conditional color rules evaluated against the displayed
            value (last match wins). Falls back to color, then the default text color
            when no rule matches.
        background_chart (ClickStackBackgroundChart): The model property of type
            ClickStackBackgroundChart.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "display_type": "displayType",
        "source_id": "sourceId",
        "select": "select",
        "formulas": "formulas",
        "number_format": "numberFormat",
        "color": "color",
        "color_rules": "colorRules",
        "background_chart": "backgroundChart",
    }

    _optionals = [
        "formulas",
        "number_format",
        "color",
        "color_rules",
        "background_chart",
    ]

    def __init__(
        self,
        source_id=None,
        select=None,
        formulas=APIHelper.SKIP,
        number_format=APIHelper.SKIP,
        color=APIHelper.SKIP,
        color_rules=APIHelper.SKIP,
        background_chart=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickStackNumberBuilderChartConfig instance."""
        # Initialize members of the class
        self.display_type = "number"
        self.source_id = source_id
        self.select = select
        if formulas is not APIHelper.SKIP:
            self.formulas = formulas
        if number_format is not APIHelper.SKIP:
            self.number_format = number_format
        if color is not APIHelper.SKIP:
            self.color = color
        if color_rules is not APIHelper.SKIP:
            self.color_rules = color_rules
        if background_chart is not APIHelper.SKIP:
            self.background_chart = background_chart

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from openapispecforclickhousecloud.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        source_id =\
            dictionary.get("sourceId")\
            if dictionary.get("sourceId")\
                else None
        select = None
        if dictionary.get("select") is not None:
            select = [
                ClickStackSelectItem.from_dictionary(x)
                    for x in dictionary.get("select")
            ]
        formulas = None
        if dictionary.get("formulas") is not None:
            formulas = [
                ClickStackFormula.from_dictionary(x)
                    for x in dictionary.get("formulas")
            ]
        else:
            formulas = APIHelper.SKIP
        number_format =\
            ClickStackNumberFormat.from_dictionary(
                dictionary.get("numberFormat"))\
                if "numberFormat" in dictionary.keys()\
                else APIHelper.SKIP
        color =\
            dictionary.get("color")\
            if dictionary.get("color")\
                else APIHelper.SKIP
        color_rules = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("ClickStackNumberTileColorCondition"),
            dictionary.get("colorRules"),
            False)\
            if dictionary.get("colorRules") is not None\
            else APIHelper.SKIP
        background_chart =\
            ClickStackBackgroundChart.from_dictionary(
                dictionary.get("backgroundChart"))\
                if "backgroundChart" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(source_id,
                   select,
                   formulas,
                   number_format,
                   color,
                   color_rules,
                   background_chart,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.display_type,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.source_id,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.select,
                    type_callable=lambda value:
                        ClickStackSelectItem.validate(value),
                    is_model_dict=True,
                    is_inner_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("displayType"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("sourceId"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("select"),
                type_callable=lambda value:
                    ClickStackSelectItem.validate(value),
                is_model_dict=True,
                is_inner_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _display_type=self.display_type
        _source_id=self.source_id
        _select=self.select
        _formulas=(
            self.formulas
            if hasattr(self, "formulas")
            else None
        )
        _number_format=(
            self.number_format
            if hasattr(self, "number_format")
            else None
        )
        _color=(
            self.color
            if hasattr(self, "color")
            else None
        )
        _color_rules=(
            self.color_rules
            if hasattr(self, "color_rules")
            else None
        )
        _background_chart=(
            self.background_chart
            if hasattr(self, "background_chart")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"display_type={_display_type!r}, "
            f"source_id={_source_id!r}, "
            f"select={_select!r}, "
            f"formulas={_formulas!r}, "
            f"number_format={_number_format!r}, "
            f"color={_color!r}, "
            f"color_rules={_color_rules!r}, "
            f"background_chart={_background_chart!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _display_type=self.display_type
        _source_id=self.source_id
        _select=self.select
        _formulas=(
            self.formulas
            if hasattr(self, "formulas")
            else None
        )
        _number_format=(
            self.number_format
            if hasattr(self, "number_format")
            else None
        )
        _color=(
            self.color
            if hasattr(self, "color")
            else None
        )
        _color_rules=(
            self.color_rules
            if hasattr(self, "color_rules")
            else None
        )
        _background_chart=(
            self.background_chart
            if hasattr(self, "background_chart")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"display_type={_display_type!s}, "
            f"source_id={_source_id!s}, "
            f"select={_select!s}, "
            f"formulas={_formulas!s}, "
            f"number_format={_number_format!s}, "
            f"color={_color!s}, "
            f"color_rules={_color_rules!s}, "
            f"background_chart={_background_chart!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
