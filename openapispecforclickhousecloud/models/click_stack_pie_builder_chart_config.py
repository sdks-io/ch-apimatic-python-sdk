"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.click_stack_number_format import (
    ClickStackNumberFormat,
)
from openapispecforclickhousecloud.models.click_stack_select_item import (
    ClickStackSelectItem,
)


class ClickStackPieBuilderChartConfig(object):
    """Implementation of the 'ClickStackPieBuilderChartConfig' model.

    Attributes:
        display_type (str): Display type discriminator. Must be "pie" for pie charts.
        source_id (str): ID of the data source to query.
        select (List[ClickStackSelectItem]): Exactly one aggregated value used to
            size each pie slice.
        group_by (str): Field expression to group results by (one slice per group
            value).
        order_by (str): Optional custom SQL ORDER BY expression (raw SQL). Overrides
            the default value-descending ordering and, when combined with "limit",
            controls which slices are kept.
        number_format (ClickStackNumberFormat): The model property of type
            ClickStackNumberFormat.
        limit (int): Maximum number of slices (SQL LIMIT). Without a custom "orderBy"
            the query keeps the groups with the largest aggregated values; with an
            "orderBy" it keeps the first slices in that order. Omit or set 0 to fetch
            all groups.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "display_type": "displayType",
        "source_id": "sourceId",
        "select": "select",
        "group_by": "groupBy",
        "order_by": "orderBy",
        "number_format": "numberFormat",
        "limit": "limit",
    }

    _optionals = [
        "group_by",
        "order_by",
        "number_format",
        "limit",
    ]

    def __init__(
        self,
        source_id=None,
        select=None,
        group_by=APIHelper.SKIP,
        order_by=APIHelper.SKIP,
        number_format=APIHelper.SKIP,
        limit=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickStackPieBuilderChartConfig instance."""
        # Initialize members of the class
        self.display_type = "pie"
        self.source_id = source_id
        self.select = select
        if group_by is not APIHelper.SKIP:
            self.group_by = group_by
        if order_by is not APIHelper.SKIP:
            self.order_by = order_by
        if number_format is not APIHelper.SKIP:
            self.number_format = number_format
        if limit is not APIHelper.SKIP:
            self.limit = limit

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        source_id =\
            dictionary.get("sourceId")\
            if dictionary.get("sourceId")\
                else None
        select = None
        if dictionary.get("select") is not None:
            select = [
                ClickStackSelectItem.from_dictionary(x)
                    for x in dictionary.get("select")
            ]
        group_by =\
            dictionary.get("groupBy")\
            if dictionary.get("groupBy")\
                else APIHelper.SKIP
        order_by =\
            dictionary.get("orderBy")\
            if dictionary.get("orderBy")\
                else APIHelper.SKIP
        number_format =\
            ClickStackNumberFormat.from_dictionary(
                dictionary.get("numberFormat"))\
                if "numberFormat" in dictionary.keys()\
                else APIHelper.SKIP
        limit =\
            dictionary.get("limit")\
            if dictionary.get("limit")\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(source_id,
                   select,
                   group_by,
                   order_by,
                   number_format,
                   limit,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.display_type,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.source_id,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.select,
                    type_callable=lambda value:
                        ClickStackSelectItem.validate(value),
                    is_model_dict=True,
                    is_inner_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("displayType"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("sourceId"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("select"),
                type_callable=lambda value:
                    ClickStackSelectItem.validate(value),
                is_model_dict=True,
                is_inner_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _display_type=self.display_type
        _source_id=self.source_id
        _select=self.select
        _group_by=(
            self.group_by
            if hasattr(self, "group_by")
            else None
        )
        _order_by=(
            self.order_by
            if hasattr(self, "order_by")
            else None
        )
        _number_format=(
            self.number_format
            if hasattr(self, "number_format")
            else None
        )
        _limit=(
            self.limit
            if hasattr(self, "limit")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"display_type={_display_type!r}, "
            f"source_id={_source_id!r}, "
            f"select={_select!r}, "
            f"group_by={_group_by!r}, "
            f"order_by={_order_by!r}, "
            f"number_format={_number_format!r}, "
            f"limit={_limit!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _display_type=self.display_type
        _source_id=self.source_id
        _select=self.select
        _group_by=(
            self.group_by
            if hasattr(self, "group_by")
            else None
        )
        _order_by=(
            self.order_by
            if hasattr(self, "order_by")
            else None
        )
        _number_format=(
            self.number_format
            if hasattr(self, "number_format")
            else None
        )
        _limit=(
            self.limit
            if hasattr(self, "limit")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"display_type={_display_type!s}, "
            f"source_id={_source_id!s}, "
            f"select={_select!s}, "
            f"group_by={_group_by!s}, "
            f"order_by={_order_by!s}, "
            f"number_format={_number_format!s}, "
            f"limit={_limit!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
