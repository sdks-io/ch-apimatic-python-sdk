"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)


class ClickPipesGcpWorkloadIdentityContext(object):
    """Implementation of the 'ClickPipesGcpWorkloadIdentityContext' model.

    Attributes:
        supported (bool): Whether the ClickPipes deployment supports GCP workload
            identity, which is in Private Preview. The principal field identifies the
            GCP service account used for source access.
        ready (bool): Whether the service tenant identity is ready for workload
            identity authentication.
        principal (str): GCP service account used by ClickPipes for workload identity
            authentication. Grant this service account access to customer source
            resources.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "supported": "supported",
        "ready": "ready",
        "principal": "principal",
    }

    _optionals = [
        "supported",
        "ready",
        "principal",
    ]

    _nullables = [
        "ready",
        "principal",
    ]

    def __init__(
        self,
        supported=APIHelper.SKIP,
        ready=APIHelper.SKIP,
        principal=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickPipesGcpWorkloadIdentityContext instance."""
        # Initialize members of the class
        if supported is not APIHelper.SKIP:
            self.supported = supported
        if ready is not APIHelper.SKIP:
            self.ready = ready
        if principal is not APIHelper.SKIP:
            self.principal = principal

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        supported =\
            dictionary.get("supported")\
            if "supported" in dictionary.keys()\
                else APIHelper.SKIP
        ready =\
            dictionary.get("ready")\
            if "ready" in dictionary.keys()\
                else APIHelper.SKIP
        principal =\
            dictionary.get("principal")\
            if "principal" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(supported,
                   ready,
                   principal,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _supported=(
            self.supported
            if hasattr(self, "supported")
            else None
        )
        _ready=(
            self.ready
            if hasattr(self, "ready")
            else None
        )
        _principal=(
            self.principal
            if hasattr(self, "principal")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"supported={_supported!r}, "
            f"ready={_ready!r}, "
            f"principal={_principal!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _supported=(
            self.supported
            if hasattr(self, "supported")
            else None
        )
        _ready=(
            self.ready
            if hasattr(self, "ready")
            else None
        )
        _principal=(
            self.principal
            if hasattr(self, "principal")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"supported={_supported!s}, "
            f"ready={_ready!s}, "
            f"principal={_principal!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
