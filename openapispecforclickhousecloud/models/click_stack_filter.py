"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)


class ClickStackFilter(object):
    """Implementation of the 'ClickStackFilter' model.

    Attributes:
        mtype (str): Filter type. Must be "QUERY_EXPRESSION".
        name (str): Display name for the dashboard filter key
        expression (str): SQL expression used when querying values for this filter,
            and when applying this dashboard filter to tiles.
        source_id (str): Source ID this dashboard filter key applies to
        source_metric_type (SourceMetricType): Metric type when source is metrics
        where (str): Optional WHERE condition to scope which rows this filter key
            reads values from
        where_language (WhereLanguage10): Language of the where condition
        applies_to_source_ids (List[str]): Optional list of source IDs this filter
            applies to. Omit or provide an empty array to apply the filter to ALL
            tiles regardless of source. A non-empty array restricts the filter to
            only tiles whose source ID is in the list; tiles using other sources are
            not affected by the selected filter value(s). Scopes the broadcast
            condition only, so a non-empty array is rejected when isBroadcastEnabled
            is false, and is omitted from responses for such a filter.
        is_broadcast_enabled (bool): Whether the selected value is applied as a
            filter condition on every builder tile this filter applies to (see
            appliesToSourceIds), and every raw sql tile using the $__filters macro.
            Omitting the field means enabled.
        is_variable_enabled (bool): Whether the selected value is exposed to tile
            queries as a dashboard variable named by variableName. Tiles may
            reference it as `$variableName` or using the (preferred)
            `$__filter($<variableName>)` and `$__conditionalAll(<condition>,
            $<variableName>)` macros.
        variable_name (str): Token tiles reference this filter's selected value by,
            as `$variableName`. Must start with a letter and may contain only
            letters, numbers, and underscores. Defaults to the display name with
            whitespace replaced by underscores and remaining illegal characters
            removed, so a variable-enabled filter whose name derives nothing usable
            must send this field explicitly. Variable names must be unique across a
            dashboard's variable-enabled filters. Names the variable only, so the
            field is rejected when isVariableEnabled is not true, and is omitted from
            responses for such a filter.
        id (str): Unique dashboard filter key ID
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "mtype": "type",
        "name": "name",
        "expression": "expression",
        "source_id": "sourceId",
        "id": "id",
        "source_metric_type": "sourceMetricType",
        "where": "where",
        "where_language": "whereLanguage",
        "applies_to_source_ids": "appliesToSourceIds",
        "is_broadcast_enabled": "isBroadcastEnabled",
        "is_variable_enabled": "isVariableEnabled",
        "variable_name": "variableName",
    }

    _optionals = [
        "source_metric_type",
        "where",
        "where_language",
        "applies_to_source_ids",
        "is_broadcast_enabled",
        "is_variable_enabled",
        "variable_name",
    ]

    def __init__(
        self,
        name=None,
        expression=None,
        source_id=None,
        id=None,
        source_metric_type=APIHelper.SKIP,
        where=APIHelper.SKIP,
        where_language=APIHelper.SKIP,
        applies_to_source_ids=APIHelper.SKIP,
        is_broadcast_enabled=APIHelper.SKIP,
        is_variable_enabled=APIHelper.SKIP,
        variable_name=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickStackFilter instance."""
        # Initialize members of the class
        self.mtype = "QUERY_EXPRESSION"
        self.name = name
        self.expression = expression
        self.source_id = source_id
        if source_metric_type is not APIHelper.SKIP:
            self.source_metric_type = source_metric_type
        if where is not APIHelper.SKIP:
            self.where = where
        if where_language is not APIHelper.SKIP:
            self.where_language = where_language
        if applies_to_source_ids is not APIHelper.SKIP:
            self.applies_to_source_ids = applies_to_source_ids
        if is_broadcast_enabled is not APIHelper.SKIP:
            self.is_broadcast_enabled = is_broadcast_enabled
        if is_variable_enabled is not APIHelper.SKIP:
            self.is_variable_enabled = is_variable_enabled
        if variable_name is not APIHelper.SKIP:
            self.variable_name = variable_name
        self.id = id

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else None
        expression =\
            dictionary.get("expression")\
            if dictionary.get("expression")\
                else None
        source_id =\
            dictionary.get("sourceId")\
            if dictionary.get("sourceId")\
                else None
        id =\
            dictionary.get("id")\
            if dictionary.get("id")\
                else None
        source_metric_type =\
            dictionary.get("sourceMetricType")\
            if dictionary.get("sourceMetricType")\
                else APIHelper.SKIP
        where =\
            dictionary.get("where")\
            if dictionary.get("where")\
                else APIHelper.SKIP
        where_language =\
            dictionary.get("whereLanguage")\
            if dictionary.get("whereLanguage")\
                else APIHelper.SKIP
        applies_to_source_ids =\
            dictionary.get("appliesToSourceIds")\
            if dictionary.get("appliesToSourceIds")\
                else APIHelper.SKIP
        is_broadcast_enabled =\
            dictionary.get("isBroadcastEnabled")\
            if "isBroadcastEnabled" in dictionary.keys()\
                else APIHelper.SKIP
        is_variable_enabled =\
            dictionary.get("isVariableEnabled")\
            if "isVariableEnabled" in dictionary.keys()\
                else APIHelper.SKIP
        variable_name =\
            dictionary.get("variableName")\
            if dictionary.get("variableName")\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(name,
                   expression,
                   source_id,
                   id,
                   source_metric_type,
                   where,
                   where_language,
                   applies_to_source_ids,
                   is_broadcast_enabled,
                   is_variable_enabled,
                   variable_name,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.mtype,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.name,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.expression,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.source_id,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.id,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("type"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("name"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("expression"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("sourceId"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("id"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _mtype=self.mtype
        _name=self.name
        _expression=self.expression
        _source_id=self.source_id
        _source_metric_type=(
            self.source_metric_type
            if hasattr(self, "source_metric_type")
            else None
        )
        _where=(
            self.where
            if hasattr(self, "where")
            else None
        )
        _where_language=(
            self.where_language
            if hasattr(self, "where_language")
            else None
        )
        _applies_to_source_ids=(
            self.applies_to_source_ids
            if hasattr(self, "applies_to_source_ids")
            else None
        )
        _is_broadcast_enabled=(
            self.is_broadcast_enabled
            if hasattr(self, "is_broadcast_enabled")
            else None
        )
        _is_variable_enabled=(
            self.is_variable_enabled
            if hasattr(self, "is_variable_enabled")
            else None
        )
        _variable_name=(
            self.variable_name
            if hasattr(self, "variable_name")
            else None
        )
        _id=self.id
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!r}, "
            f"name={_name!r}, "
            f"expression={_expression!r}, "
            f"source_id={_source_id!r}, "
            f"source_metric_type={_source_metric_type!r}, "
            f"where={_where!r}, "
            f"where_language={_where_language!r}, "
            f"applies_to_source_ids={_applies_to_source_ids!r}, "
            f"is_broadcast_enabled={_is_broadcast_enabled!r}, "
            f"is_variable_enabled={_is_variable_enabled!r}, "
            f"variable_name={_variable_name!r}, "
            f"id={_id!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _mtype=self.mtype
        _name=self.name
        _expression=self.expression
        _source_id=self.source_id
        _source_metric_type=(
            self.source_metric_type
            if hasattr(self, "source_metric_type")
            else None
        )
        _where=(
            self.where
            if hasattr(self, "where")
            else None
        )
        _where_language=(
            self.where_language
            if hasattr(self, "where_language")
            else None
        )
        _applies_to_source_ids=(
            self.applies_to_source_ids
            if hasattr(self, "applies_to_source_ids")
            else None
        )
        _is_broadcast_enabled=(
            self.is_broadcast_enabled
            if hasattr(self, "is_broadcast_enabled")
            else None
        )
        _is_variable_enabled=(
            self.is_variable_enabled
            if hasattr(self, "is_variable_enabled")
            else None
        )
        _variable_name=(
            self.variable_name
            if hasattr(self, "variable_name")
            else None
        )
        _id=self.id
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"mtype={_mtype!s}, "
            f"name={_name!s}, "
            f"expression={_expression!s}, "
            f"source_id={_source_id!s}, "
            f"source_metric_type={_source_metric_type!s}, "
            f"where={_where!s}, "
            f"where_language={_where_language!s}, "
            f"applies_to_source_ids={_applies_to_source_ids!s}, "
            f"is_broadcast_enabled={_is_broadcast_enabled!s}, "
            f"is_variable_enabled={_is_variable_enabled!s}, "
            f"variable_name={_variable_name!s}, "
            f"id={_id!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
