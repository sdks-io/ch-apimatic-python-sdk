"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.credit_balance import (
    CreditBalance,
)


class CreditBalances(object):
    """Implementation of the 'CreditBalances' model.

    Attributes:
        total_remaining_credits (float): Total remaining credits across all active
            balances, in ClickHouse Credits (CHCs).
        balances (List[CreditBalance]): List of active balances for the organization.
            Empty when the organization has none.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "total_remaining_credits": "totalRemainingCredits",
        "balances": "balances",
    }

    _optionals = [
        "total_remaining_credits",
        "balances",
    ]

    def __init__(
        self,
        total_remaining_credits=APIHelper.SKIP,
        balances=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a CreditBalances instance."""
        # Initialize members of the class
        if total_remaining_credits is not APIHelper.SKIP:
            self.total_remaining_credits = total_remaining_credits
        if balances is not APIHelper.SKIP:
            self.balances = balances

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        total_remaining_credits =\
            dictionary.get("totalRemainingCredits")\
            if dictionary.get("totalRemainingCredits")\
                else APIHelper.SKIP
        balances = None
        if dictionary.get("balances") is not None:
            balances = [
                CreditBalance.from_dictionary(x)
                    for x in dictionary.get("balances")
            ]
        else:
            balances = APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(total_remaining_credits,
                   balances,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _total_remaining_credits=(
            self.total_remaining_credits
            if hasattr(self, "total_remaining_credits")
            else None
        )
        _balances=(
            self.balances
            if hasattr(self, "balances")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"total_remaining_credits={_total_remaining_credits!r}, "
            f"balances={_balances!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _total_remaining_credits=(
            self.total_remaining_credits
            if hasattr(self, "total_remaining_credits")
            else None
        )
        _balances=(
            self.balances
            if hasattr(self, "balances")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"total_remaining_credits={_total_remaining_credits!s}, "
            f"balances={_balances!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
