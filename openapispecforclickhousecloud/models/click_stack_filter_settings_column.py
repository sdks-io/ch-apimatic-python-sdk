"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)


class ClickStackFilterSettingsColumn(object):
    """Implementation of the 'ClickStackFilterSettingsColumn' model.

    Attributes:
        name (str): Column of the source's table that selected filter values are
            matched against. Also the key the selection is persisted under in links,
            saved searches, and dashboards.
        label (str): Display label for the column
        value_expression (str): Optional SQL expression, evaluated against the
            filter-values table, that produces the available filter options. Use it
            when the options live in a differently-named column, or must be
            transformed to match the values stored in the source table. Defaults to
            reading `name` as a plain column when omitted.
        allow_all (bool): Whether to offer an "All" option that expands to every
            available value at query time. Best suited to low-cardinality columns.
            Defaults to false.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "name": "name",
        "label": "label",
        "value_expression": "valueExpression",
        "allow_all": "allowAll",
    }

    _optionals = [
        "value_expression",
        "allow_all",
    ]

    _nullables = [
        "value_expression",
    ]

    def __init__(
        self,
        name=None,
        label=None,
        value_expression=APIHelper.SKIP,
        allow_all=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickStackFilterSettingsColumn instance."""
        # Initialize members of the class
        self.name = name
        self.label = label
        if value_expression is not APIHelper.SKIP:
            self.value_expression = value_expression
        if allow_all is not APIHelper.SKIP:
            self.allow_all = allow_all

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else None
        label =\
            dictionary.get("label")\
            if dictionary.get("label")\
                else None
        value_expression =\
            dictionary.get("valueExpression")\
            if "valueExpression" in dictionary.keys()\
                else APIHelper.SKIP
        allow_all =\
            dictionary.get("allowAll")\
            if "allowAll" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(name,
                   label,
                   value_expression,
                   allow_all,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.name,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.label,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("name"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("label"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _name=self.name
        _label=self.label
        _value_expression=(
            self.value_expression
            if hasattr(self, "value_expression")
            else None
        )
        _allow_all=(
            self.allow_all
            if hasattr(self, "allow_all")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"name={_name!r}, "
            f"label={_label!r}, "
            f"value_expression={_value_expression!r}, "
            f"allow_all={_allow_all!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _name=self.name
        _label=self.label
        _value_expression=(
            self.value_expression
            if hasattr(self, "value_expression")
            else None
        )
        _allow_all=(
            self.allow_all
            if hasattr(self, "allow_all")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"name={_name!s}, "
            f"label={_label!s}, "
            f"value_expression={_value_expression!s}, "
            f"allow_all={_allow_all!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
