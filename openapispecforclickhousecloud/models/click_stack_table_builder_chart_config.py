"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.click_stack_formula import (
    ClickStackFormula,
)
from openapispecforclickhousecloud.models.click_stack_number_format import (
    ClickStackNumberFormat,
)
from openapispecforclickhousecloud.models.click_stack_select_item import (
    ClickStackSelectItem,
)


class ClickStackTableBuilderChartConfig(object):
    """Implementation of the 'ClickStackTableBuilderChartConfig' model.

    Attributes:
        display_type (str): Display type discriminator. Must be "table" for table
            charts.
        source_id (str): ID of the data source to query.
        select (List[ClickStackSelectItem]): One or more aggregated values to display
            as table columns. When asRatio is true, exactly two select items are
            required.
        group_by (str): Field expression to group results by (one row per group
            value).
        having (str): Post-aggregation SQL HAVING condition.
        order_by (str): SQL ORDER BY expression for sorting table rows.
        as_ratio (bool): Display select[0] / select[1] as a ratio. Requires exactly
            two select items.
        number_format (ClickStackNumberFormat): The model property of type
            ClickStackNumberFormat.
        group_by_columns_on_left (bool): When true, render Group By columns to the
            left of series columns in the table. Defaults to false (Group By columns
            on the right).
        on_click (ClickStackOnClickSearch | ClickStackOnClickDashboard |
            ClickStackOnClickExternal | None): The model property of type
            ClickStackOnClickSearch | ClickStackOnClickDashboard |
            ClickStackOnClickExternal | None.
        formulas (List[ClickStackFormula]): Derived columns computed from the select
            items via letter-ref arithmetic ("A" = select[0], "B" = select[1], ...).
            Metric, log, and trace sources only. Cannot be combined with asRatio.
        show_operand_series (bool): Only meaningful with formulas. When false, only
            the formula columns are returned; the raw operand columns are hidden.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "display_type": "displayType",
        "source_id": "sourceId",
        "select": "select",
        "group_by": "groupBy",
        "having": "having",
        "order_by": "orderBy",
        "as_ratio": "asRatio",
        "number_format": "numberFormat",
        "group_by_columns_on_left": "groupByColumnsOnLeft",
        "on_click": "onClick",
        "formulas": "formulas",
        "show_operand_series": "showOperandSeries",
    }

    _optionals = [
        "group_by",
        "having",
        "order_by",
        "as_ratio",
        "number_format",
        "group_by_columns_on_left",
        "on_click",
        "formulas",
        "show_operand_series",
    ]

    def __init__(
        self,
        source_id=None,
        select=None,
        group_by=APIHelper.SKIP,
        having=APIHelper.SKIP,
        order_by=APIHelper.SKIP,
        as_ratio=APIHelper.SKIP,
        number_format=APIHelper.SKIP,
        group_by_columns_on_left=APIHelper.SKIP,
        on_click=APIHelper.SKIP,
        formulas=APIHelper.SKIP,
        show_operand_series=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickStackTableBuilderChartConfig instance."""
        # Initialize members of the class
        self.display_type = "table"
        self.source_id = source_id
        self.select = select
        if group_by is not APIHelper.SKIP:
            self.group_by = group_by
        if having is not APIHelper.SKIP:
            self.having = having
        if order_by is not APIHelper.SKIP:
            self.order_by = order_by
        if as_ratio is not APIHelper.SKIP:
            self.as_ratio = as_ratio
        if number_format is not APIHelper.SKIP:
            self.number_format = number_format
        if group_by_columns_on_left is not APIHelper.SKIP:
            self.group_by_columns_on_left = group_by_columns_on_left
        if on_click is not APIHelper.SKIP:
            self.on_click = on_click
        if formulas is not APIHelper.SKIP:
            self.formulas = formulas
        if show_operand_series is not APIHelper.SKIP:
            self.show_operand_series = show_operand_series

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from openapispecforclickhousecloud.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        source_id =\
            dictionary.get("sourceId")\
            if dictionary.get("sourceId")\
                else None
        select = None
        if dictionary.get("select") is not None:
            select = [
                ClickStackSelectItem.from_dictionary(x)
                    for x in dictionary.get("select")
            ]
        group_by =\
            dictionary.get("groupBy")\
            if dictionary.get("groupBy")\
                else APIHelper.SKIP
        having =\
            dictionary.get("having")\
            if dictionary.get("having")\
                else APIHelper.SKIP
        order_by =\
            dictionary.get("orderBy")\
            if dictionary.get("orderBy")\
                else APIHelper.SKIP
        as_ratio =\
            dictionary.get("asRatio")\
            if "asRatio" in dictionary.keys()\
                else APIHelper.SKIP
        number_format =\
            ClickStackNumberFormat.from_dictionary(
                dictionary.get("numberFormat"))\
                if "numberFormat" in dictionary.keys()\
                else APIHelper.SKIP
        group_by_columns_on_left =\
            dictionary.get("groupByColumnsOnLeft")\
            if "groupByColumnsOnLeft" in dictionary.keys()\
                else APIHelper.SKIP
        on_click = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("ClickStackOnClick"),
            dictionary.get("onClick"),
            False)\
            if dictionary.get("onClick") is not None\
            else APIHelper.SKIP
        formulas = None
        if dictionary.get("formulas") is not None:
            formulas = [
                ClickStackFormula.from_dictionary(x)
                    for x in dictionary.get("formulas")
            ]
        else:
            formulas = APIHelper.SKIP
        show_operand_series =\
            dictionary.get("showOperandSeries")\
            if "showOperandSeries" in dictionary.keys()\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(source_id,
                   select,
                   group_by,
                   having,
                   order_by,
                   as_ratio,
                   number_format,
                   group_by_columns_on_left,
                   on_click,
                   formulas,
                   show_operand_series,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.display_type,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.source_id,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.select,
                    type_callable=lambda value:
                        ClickStackSelectItem.validate(value),
                    is_model_dict=True,
                    is_inner_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("displayType"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("sourceId"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("select"),
                type_callable=lambda value:
                    ClickStackSelectItem.validate(value),
                is_model_dict=True,
                is_inner_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _display_type=self.display_type
        _source_id=self.source_id
        _select=self.select
        _group_by=(
            self.group_by
            if hasattr(self, "group_by")
            else None
        )
        _having=(
            self.having
            if hasattr(self, "having")
            else None
        )
        _order_by=(
            self.order_by
            if hasattr(self, "order_by")
            else None
        )
        _as_ratio=(
            self.as_ratio
            if hasattr(self, "as_ratio")
            else None
        )
        _number_format=(
            self.number_format
            if hasattr(self, "number_format")
            else None
        )
        _group_by_columns_on_left=(
            self.group_by_columns_on_left
            if hasattr(self, "group_by_columns_on_left")
            else None
        )
        _on_click=(
            self.on_click
            if hasattr(self, "on_click")
            else None
        )
        _formulas=(
            self.formulas
            if hasattr(self, "formulas")
            else None
        )
        _show_operand_series=(
            self.show_operand_series
            if hasattr(self, "show_operand_series")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"display_type={_display_type!r}, "
            f"source_id={_source_id!r}, "
            f"select={_select!r}, "
            f"group_by={_group_by!r}, "
            f"having={_having!r}, "
            f"order_by={_order_by!r}, "
            f"as_ratio={_as_ratio!r}, "
            f"number_format={_number_format!r}, "
            f"group_by_columns_on_left={_group_by_columns_on_left!r}, "
            f"on_click={_on_click!r}, "
            f"formulas={_formulas!r}, "
            f"show_operand_series={_show_operand_series!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _display_type=self.display_type
        _source_id=self.source_id
        _select=self.select
        _group_by=(
            self.group_by
            if hasattr(self, "group_by")
            else None
        )
        _having=(
            self.having
            if hasattr(self, "having")
            else None
        )
        _order_by=(
            self.order_by
            if hasattr(self, "order_by")
            else None
        )
        _as_ratio=(
            self.as_ratio
            if hasattr(self, "as_ratio")
            else None
        )
        _number_format=(
            self.number_format
            if hasattr(self, "number_format")
            else None
        )
        _group_by_columns_on_left=(
            self.group_by_columns_on_left
            if hasattr(self, "group_by_columns_on_left")
            else None
        )
        _on_click=(
            self.on_click
            if hasattr(self, "on_click")
            else None
        )
        _formulas=(
            self.formulas
            if hasattr(self, "formulas")
            else None
        )
        _show_operand_series=(
            self.show_operand_series
            if hasattr(self, "show_operand_series")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"display_type={_display_type!s}, "
            f"source_id={_source_id!s}, "
            f"select={_select!s}, "
            f"group_by={_group_by!s}, "
            f"having={_having!s}, "
            f"order_by={_order_by!s}, "
            f"as_ratio={_as_ratio!s}, "
            f"number_format={_number_format!s}, "
            f"group_by_columns_on_left={_group_by_columns_on_left!s}, "
            f"on_click={_on_click!s}, "
            f"formulas={_formulas!s}, "
            f"show_operand_series={_show_operand_series!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
