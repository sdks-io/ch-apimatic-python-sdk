"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.click_stack_dashboard_container_tab import (
    ClickStackDashboardContainerTab,
)


class ClickStackDashboardContainer(object):
    """Implementation of the 'ClickStackDashboardContainer' model.

    Attributes:
        id (str): Unique identifier for the container within the dashboard.
        title (str): Display title for the container.
        collapsed (bool): Persisted default collapse state. Per-viewer state lives in
            the URL.
        collapsible (bool): Whether the user can collapse the group.
        bordered (bool): Whether to show a visual border around the group.
        tabs (List[ClickStackDashboardContainerTab]): Optional tabs. 2+ entries
            renders a tab bar; 0-1 entries renders a plain group header. Tiles join a
            tab via tabId.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "id": "id",
        "title": "title",
        "collapsed": "collapsed",
        "collapsible": "collapsible",
        "bordered": "bordered",
        "tabs": "tabs",
    }

    _optionals = [
        "collapsible",
        "bordered",
        "tabs",
    ]

    def __init__(
        self,
        id=None,
        title=None,
        collapsed=None,
        collapsible=APIHelper.SKIP,
        bordered=APIHelper.SKIP,
        tabs=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickStackDashboardContainer instance."""
        # Initialize members of the class
        self.id = id
        self.title = title
        self.collapsed = collapsed
        if collapsible is not APIHelper.SKIP:
            self.collapsible = collapsible
        if bordered is not APIHelper.SKIP:
            self.bordered = bordered
        if tabs is not APIHelper.SKIP:
            self.tabs = tabs

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        id =\
            dictionary.get("id")\
            if dictionary.get("id")\
                else None
        title =\
            dictionary.get("title")\
            if dictionary.get("title")\
                else None
        collapsed =\
            dictionary.get("collapsed")\
            if "collapsed" in dictionary.keys()\
                else None
        collapsible =\
            dictionary.get("collapsible")\
            if "collapsible" in dictionary.keys()\
                else APIHelper.SKIP
        bordered =\
            dictionary.get("bordered")\
            if "bordered" in dictionary.keys()\
                else APIHelper.SKIP
        tabs = None
        if dictionary.get("tabs") is not None:
            tabs = [
                ClickStackDashboardContainerTab.from_dictionary(x)
                    for x in dictionary.get("tabs")
            ]
        else:
            tabs = APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(id,
                   title,
                   collapsed,
                   collapsible,
                   bordered,
                   tabs,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.id,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.title,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.collapsed,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        bool,
                ))

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("id"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("title"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("collapsed"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    bool,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _id=self.id
        _title=self.title
        _collapsed=self.collapsed
        _collapsible=(
            self.collapsible
            if hasattr(self, "collapsible")
            else None
        )
        _bordered=(
            self.bordered
            if hasattr(self, "bordered")
            else None
        )
        _tabs=(
            self.tabs
            if hasattr(self, "tabs")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"id={_id!r}, "
            f"title={_title!r}, "
            f"collapsed={_collapsed!r}, "
            f"collapsible={_collapsible!r}, "
            f"bordered={_bordered!r}, "
            f"tabs={_tabs!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _id=self.id
        _title=self.title
        _collapsed=self.collapsed
        _collapsible=(
            self.collapsible
            if hasattr(self, "collapsible")
            else None
        )
        _bordered=(
            self.bordered
            if hasattr(self, "bordered")
            else None
        )
        _tabs=(
            self.tabs
            if hasattr(self, "tabs")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"id={_id!s}, "
            f"title={_title!s}, "
            f"collapsed={_collapsed!s}, "
            f"collapsible={_collapsible!s}, "
            f"bordered={_bordered!s}, "
            f"tabs={_tabs!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
