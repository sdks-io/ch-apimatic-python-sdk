"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501

class Authentication2(object):
    """Implementation of the 'Authentication2' enum.

    Authentication method of the Kafka source. SERVICE_ACCOUNT_WORKLOAD_IDENTITY is
    in Private Preview. ClickPipes uses the GCP service account returned in
    gcpWorkloadIdentity.principal by the operation with operationId
    clickPipesServiceContextGet; grant it access to the source resources. Supported
    authentication methods: kafka: PLAIN, SCRAM-SHA-256, SCRAM-SHA-512, MUTUAL_TLS,
    msk: SCRAM-SHA-512, IAM_ROLE, IAM_USER, MUTUAL_TLS, gcmk: PLAIN, MUTUAL_TLS,
    SERVICE_ACCOUNT_WORKLOAD_IDENTITY, confluent: PLAIN, MUTUAL_TLS, warpstream:
    PLAIN, azureeventhub: PLAIN, redpanda: SCRAM-SHA-256, SCRAM-SHA-512, MUTUAL_TLS,
    dokafka: SCRAM-SHA-256, MUTUAL_TLS

    Attributes:
        PLAIN: The enum member of type str.
        SCRAMSHA256: The enum member of type str.
        SCRAMSHA512: The enum member of type str.
        IAM_ROLE: The enum member of type str.
        IAM_USER: The enum member of type str.
        MUTUAL_TLS: The enum member of type str.
        SERVICE_ACCOUNT_WORKLOAD_IDENTITY: The enum member of type str.

    """

    _all_values = ["PLAIN", "SCRAM-SHA-256", "SCRAM-SHA-512", "IAM_ROLE", "IAM_USER",
        "MUTUAL_TLS", "SERVICE_ACCOUNT_WORKLOAD_IDENTITY"]
    PLAIN = "PLAIN"

    SCRAMSHA256 = "SCRAM-SHA-256"

    SCRAMSHA512 = "SCRAM-SHA-512"

    IAM_ROLE = "IAM_ROLE"

    IAM_USER = "IAM_USER"

    MUTUAL_TLS = "MUTUAL_TLS"

    SERVICE_ACCOUNT_WORKLOAD_IDENTITY = "SERVICE_ACCOUNT_WORKLOAD_IDENTITY"

    @classmethod
    def validate(cls, value):
        """Validate value contains in enum

        Args:
            value: the value to be validated

        Returns:
            boolean : if value is valid enum values.

        """
        return value in cls._all_values

    @classmethod
    def from_value(cls, value, default=None):
        """Return the matching enum value for the given input."""
        if value is None:
            return default

        # If numeric and matches directly
        if isinstance(value, int):
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and val == value:
                    return val

        # If string, perform case-insensitive match
        if isinstance(value, str):
            value_lower = value.lower()
            for name, val in cls.__dict__.items():
                if not name.startswith("_") and (
                    name.lower() == value_lower or str(val).lower() == value_lower
                ):
                    return val

        # Fallback to default
        return default
