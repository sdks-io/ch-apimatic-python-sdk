"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.scaling_schedule_base_config import (
    ScalingScheduleBaseConfig,
)
from openapispecforclickhousecloud.models.scaling_schedule_entry import (
    ScalingScheduleEntry,
)


class ScalingSchedule(object):
    """Implementation of the 'ScalingSchedule' model.

    Attributes:
        entries (List[ScalingScheduleEntry]): List of schedule entries.
        base_config (ScalingScheduleBaseConfig): The model property of type
            ScalingScheduleBaseConfig.
        active_entry_id (uuid|str): ID of the currently-active schedule entry. Absent
            when no entry is active and the base config is in effect.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "entries": "entries",
        "base_config": "baseConfig",
        "active_entry_id": "activeEntryId",
    }

    _optionals = [
        "active_entry_id",
    ]

    def __init__(
        self,
        entries=None,
        base_config=None,
        active_entry_id=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ScalingSchedule instance."""
        # Initialize members of the class
        self.entries = entries
        self.base_config = base_config
        if active_entry_id is not APIHelper.SKIP:
            self.active_entry_id = active_entry_id

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        entries = None
        if dictionary.get("entries") is not None:
            entries = [
                ScalingScheduleEntry.from_dictionary(x)
                    for x in dictionary.get("entries")
            ]
        base_config =\
            ScalingScheduleBaseConfig.from_dictionary(
                dictionary.get("baseConfig"))\
                if dictionary.get("baseConfig") else None
        active_entry_id =\
            dictionary.get("activeEntryId")\
            if dictionary.get("activeEntryId")\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(entries,
                   base_config,
                   active_entry_id,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _entries=self.entries
        _base_config=self.base_config
        _active_entry_id=(
            self.active_entry_id
            if hasattr(self, "active_entry_id")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"entries={_entries!r}, "
            f"base_config={_base_config!r}, "
            f"active_entry_id={_active_entry_id!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _entries=self.entries
        _base_config=self.base_config
        _active_entry_id=(
            self.active_entry_id
            if hasattr(self, "active_entry_id")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"entries={_entries!s}, "
            f"base_config={_base_config!s}, "
            f"active_entry_id={_active_entry_id!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
