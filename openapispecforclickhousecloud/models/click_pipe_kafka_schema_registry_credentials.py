"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)


class ClickPipeKafkaSchemaRegistryCredentials(object):
    """Implementation of the 'ClickPipeKafkaSchemaRegistryCredentials' model.

    Attributes:
        username (str): Username for the schema registry.
        password (str): Password for the schema registry.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "username": "username",
        "password": "password",
    }

    _optionals = [
        "username",
        "password",
    ]

    def __init__(
        self,
        username=APIHelper.SKIP,
        password=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickPipeKafkaSchemaRegistryCredentials instance."""
        # Initialize members of the class
        if username is not APIHelper.SKIP:
            self.username = username
        if password is not APIHelper.SKIP:
            self.password = password

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        username =\
            dictionary.get("username")\
            if dictionary.get("username")\
                else APIHelper.SKIP
        password =\
            dictionary.get("password")\
            if dictionary.get("password")\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(username,
                   password,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return True

        if not isinstance(dictionary, dict):
            return False

        return True

    def __repr__(self):
        """Return a unambiguous string representation."""
        _username=(
            self.username
            if hasattr(self, "username")
            else None
        )
        _password=(
            self.password
            if hasattr(self, "password")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"username={_username!r}, "
            f"password={_password!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _username=(
            self.username
            if hasattr(self, "username")
            else None
        )
        _password=(
            self.password
            if hasattr(self, "password")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"username={_username!s}, "
            f"password={_password!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
