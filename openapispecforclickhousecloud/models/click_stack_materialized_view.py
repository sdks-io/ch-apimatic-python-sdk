"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.click_stack_aggregated_column import (
    ClickStackAggregatedColumn,
)


class ClickStackMaterializedView(object):
    """Implementation of the 'ClickStackMaterializedView' model.

    Attributes:
        database_name (str): Database name for the materialized view
        table_name (str): Table name for the materialized view
        dimension_columns (str): Columns which are not pre-aggregated in the
            materialized view and can be used for filtering and grouping.
        min_granularity (str): The granularity of the timestamp column: a positive
            integer followed by a unit (s, m, h, d). Common values: 1s, 15s, 30s, 1m,
            5m, 15m, 30m, 1h, 2h, 6h, 12h, 1d, 2d, 7d, 30d.
        min_date (datetime): (Optional) The earliest date and time for which the
            materialized view contains data. If not provided, then HyperDX will
            assume that the materialized view contains data for all dates for which
            the source table contains data.
        timestamp_column (str): Timestamp column name
        aggregated_columns (List[ClickStackAggregatedColumn]): Columns which are
            pre-aggregated by the materialized view
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "database_name": "databaseName",
        "table_name": "tableName",
        "dimension_columns": "dimensionColumns",
        "min_granularity": "minGranularity",
        "timestamp_column": "timestampColumn",
        "aggregated_columns": "aggregatedColumns",
        "min_date": "minDate",
    }

    _optionals = [
        "min_date",
    ]

    _nullables = [
        "min_date",
    ]

    def __init__(
        self,
        database_name=None,
        table_name=None,
        dimension_columns=None,
        min_granularity=None,
        timestamp_column=None,
        aggregated_columns=None,
        min_date=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickStackMaterializedView instance."""
        # Initialize members of the class
        self.database_name = database_name
        self.table_name = table_name
        self.dimension_columns = dimension_columns
        self.min_granularity = min_granularity
        if min_date is not APIHelper.SKIP:
            self.min_date =\
                 APIHelper.apply_datetime_converter(
                min_date, APIHelper.RFC3339DateTime)\
                 if min_date else None
        self.timestamp_column = timestamp_column
        self.aggregated_columns = aggregated_columns

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        database_name =\
            dictionary.get("databaseName")\
            if dictionary.get("databaseName")\
                else None
        table_name =\
            dictionary.get("tableName")\
            if dictionary.get("tableName")\
                else None
        dimension_columns =\
            dictionary.get("dimensionColumns")\
            if dictionary.get("dimensionColumns")\
                else None
        min_granularity =\
            dictionary.get("minGranularity")\
            if dictionary.get("minGranularity")\
                else None
        timestamp_column =\
            dictionary.get("timestampColumn")\
            if dictionary.get("timestampColumn")\
                else None
        aggregated_columns = None
        if dictionary.get("aggregatedColumns") is not None:
            aggregated_columns = [
                ClickStackAggregatedColumn.from_dictionary(x)
                    for x in dictionary.get("aggregatedColumns")
            ]
        if "minDate" in dictionary.keys():
            min_date = APIHelper.RFC3339DateTime.from_value(
                dictionary.get("minDate")).datetime\
                if dictionary.get("minDate") else None

        else:
            min_date = APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(database_name,
                   table_name,
                   dimension_columns,
                   min_granularity,
                   timestamp_column,
                   aggregated_columns,
                   min_date,
                   additional_properties)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        if isinstance(dictionary, cls):
            return APIHelper.is_valid_type(
                    value=dictionary.database_name,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.table_name,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.dimension_columns,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.min_granularity,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.timestamp_column,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.aggregated_columns,
                    type_callable=lambda value:
                        ClickStackAggregatedColumn.validate(value),
                    is_model_dict=True,
                    is_inner_model_dict=True)

        if not isinstance(dictionary, dict):
            return False

        return APIHelper.is_valid_type(
                value=dictionary.get("databaseName"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("tableName"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("dimensionColumns"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("minGranularity"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("timestampColumn"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("aggregatedColumns"),
                type_callable=lambda value:
                    ClickStackAggregatedColumn.validate(value),
                is_model_dict=True,
                is_inner_model_dict=True)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _database_name=self.database_name
        _table_name=self.table_name
        _dimension_columns=self.dimension_columns
        _min_granularity=self.min_granularity
        _min_date=(
            self.min_date
            if hasattr(self, "min_date")
            else None
        )
        _timestamp_column=self.timestamp_column
        _aggregated_columns=self.aggregated_columns
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"database_name={_database_name!r}, "
            f"table_name={_table_name!r}, "
            f"dimension_columns={_dimension_columns!r}, "
            f"min_granularity={_min_granularity!r}, "
            f"min_date={_min_date!r}, "
            f"timestamp_column={_timestamp_column!r}, "
            f"aggregated_columns={_aggregated_columns!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _database_name=self.database_name
        _table_name=self.table_name
        _dimension_columns=self.dimension_columns
        _min_granularity=self.min_granularity
        _min_date=(
            self.min_date
            if hasattr(self, "min_date")
            else None
        )
        _timestamp_column=self.timestamp_column
        _aggregated_columns=self.aggregated_columns
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"database_name={_database_name!s}, "
            f"table_name={_table_name!s}, "
            f"dimension_columns={_dimension_columns!s}, "
            f"min_granularity={_min_granularity!s}, "
            f"min_date={_min_date!s}, "
            f"timestamp_column={_timestamp_column!s}, "
            f"aggregated_columns={_aggregated_columns!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
