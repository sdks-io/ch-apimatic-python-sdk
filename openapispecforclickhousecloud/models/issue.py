"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)


class Issue(object):
    """Implementation of the 'Issue' model.

    Attributes:
        path (List[str | float]): Path to the invalid field.
        code (str): Validation issue code.
        message (str): Human-readable description of the issue.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "path": "path",
        "code": "code",
        "message": "message",
    }

    def __init__(
        self,
        path=None,
        code=None,
        message=None):
        """Initialize a Issue instance."""
        # Initialize members of the class
        self.path = path
        self.code = code
        self.message = message

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        from openapispecforclickhousecloud.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        path = APIHelper.deserialize_union_type(
            UnionTypeLookUp.get("IssuePath"),
            dictionary.get("path"),
            False)\
            if dictionary.get("path") is not None\
            else None
        code =\
            dictionary.get("code")\
            if dictionary.get("code")\
                else None
        message =\
            dictionary.get("message")\
            if dictionary.get("message")\
                else None

        # Return an object of this model
        return cls(path,
                   code,
                   message)

    @classmethod
    def validate(cls, dictionary):
        """Validate dictionary against class required properties

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            boolean : if dictionary is valid contains required properties.

        """
        from openapispecforclickhousecloud.utilities.union_type_lookup import (
            UnionTypeLookUp,
        )

        if isinstance(dictionary, cls):
            return (UnionTypeLookUp.get("IssuePath")
                .validate(dictionary.path).is_valid) \
                and APIHelper.is_valid_type(
                    value=dictionary.code,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                )) \
                and APIHelper.is_valid_type(
                    value=dictionary.message,
                    type_callable=lambda value:
                        isinstance(
                        value,
                        str,
                ))

        if not isinstance(dictionary, dict):
            return False

        return (UnionTypeLookUp.get("IssuePath")
            .validate(dictionary.get("path")).is_valid) \
            and APIHelper.is_valid_type(
                value=dictionary.get("code"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            )) \
            and APIHelper.is_valid_type(
                value=dictionary.get("message"),
                type_callable=lambda value:
                    isinstance(
                    value,
                    str,
            ))

    def __repr__(self):
        """Return a unambiguous string representation."""
        _path=self.path
        _code=self.code
        _message=self.message
        return (
            f"{self.__class__.__name__}("
            f"path={_path!r}, "
            f"code={_code!r}, "
            f"message={_message!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _path=self.path
        _code=self.code
        _message=self.message
        return (
            f"{self.__class__.__name__}("
            f"path={_path!s}, "
            f"code={_code!s}, "
            f"message={_message!s}, "
            f")"
        )
