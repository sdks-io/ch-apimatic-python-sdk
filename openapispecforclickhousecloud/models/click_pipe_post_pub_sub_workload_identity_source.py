"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)


class ClickPipePostPubSubWorkloadIdentitySource(object):
    """Implementation of the 'ClickPipePostPubSubWorkloadIdentitySource' model.

    Attributes:
        format (Format6): Format of messages in the Pub/Sub topic. GCP Pub/Sub
            ClickPipes are in limited preview — contact support to enable this
            feature for your organization.
        project_id (str): GCP project ID that owns the Pub/Sub topic.
        topic (str): Pub/Sub topic name (not the fully-qualified path).
        authentication (str): SERVICE_ACCOUNT_WORKLOAD_IDENTITY is in Private
            Preview. ClickPipes uses the GCP service account returned in
            gcpWorkloadIdentity.principal by the operation with operationId
            clickPipesServiceContextGet; grant it access to the source resources.
        seek_type (SeekType): Starting position strategy for consuming the
            subscription. The seekTimestamp companion is required only when seekType
            is "timestamp"; setting it for a mismatched seek type is rejected.
        seek_timestamp (datetime): RFC 3339 / ISO 8601 timestamp to seek to. Required
            when seekType is "timestamp"; must be omitted otherwise.
        filter (str): Optional Pub/Sub subscription filter expression (CEL). Maximum
            256 characters.
        enable_ordering (bool): Whether to enable ordered delivery of messages
            (requires messages to be published with ordering keys).
        ack_deadline (int): Acknowledgement deadline for messages, in seconds. Must
            be between 10 and 600.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "format": "format",
        "project_id": "projectId",
        "topic": "topic",
        "authentication": "authentication",
        "seek_type": "seekType",
        "seek_timestamp": "seekTimestamp",
        "filter": "filter",
        "enable_ordering": "enableOrdering",
        "ack_deadline": "ackDeadline",
    }

    _optionals = [
        "seek_timestamp",
        "filter",
        "enable_ordering",
        "ack_deadline",
    ]

    _nullables = [
        "seek_timestamp",
        "filter",
        "enable_ordering",
        "ack_deadline",
    ]

    def __init__(
        self,
        format=None,
        project_id=None,
        topic=None,
        seek_type=None,
        seek_timestamp=APIHelper.SKIP,
        filter=APIHelper.SKIP,
        enable_ordering=APIHelper.SKIP,
        ack_deadline=APIHelper.SKIP):
        """Initialize a ClickPipePostPubSubWorkloadIdentitySource instance."""
        # Initialize members of the class
        self.format = format
        self.project_id = project_id
        self.topic = topic
        self.authentication = "SERVICE_ACCOUNT_WORKLOAD_IDENTITY"
        self.seek_type = seek_type
        if seek_timestamp is not APIHelper.SKIP:
            self.seek_timestamp =\
                 APIHelper.apply_datetime_converter(
                seek_timestamp, APIHelper.RFC3339DateTime)\
                 if seek_timestamp else None
        if filter is not APIHelper.SKIP:
            self.filter = filter
        if enable_ordering is not APIHelper.SKIP:
            self.enable_ordering = enable_ordering
        if ack_deadline is not APIHelper.SKIP:
            self.ack_deadline = ack_deadline

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        format =\
            dictionary.get("format")\
            if dictionary.get("format")\
                else None
        project_id =\
            dictionary.get("projectId")\
            if dictionary.get("projectId")\
                else None
        topic =\
            dictionary.get("topic")\
            if dictionary.get("topic")\
                else None
        seek_type =\
            dictionary.get("seekType")\
            if dictionary.get("seekType")\
                else None
        if "seekTimestamp" in dictionary.keys():
            seek_timestamp = APIHelper.RFC3339DateTime.from_value(
                dictionary.get("seekTimestamp")).datetime\
                if dictionary.get("seekTimestamp") else None

        else:
            seek_timestamp = APIHelper.SKIP
        filter =\
            dictionary.get("filter")\
            if "filter" in dictionary.keys()\
                else APIHelper.SKIP
        enable_ordering =\
            dictionary.get("enableOrdering")\
            if "enableOrdering" in dictionary.keys()\
                else APIHelper.SKIP
        ack_deadline =\
            dictionary.get("ackDeadline")\
            if "ackDeadline" in dictionary.keys()\
                else APIHelper.SKIP

        # Return an object of this model
        return cls(format,
                   project_id,
                   topic,
                   seek_type,
                   seek_timestamp,
                   filter,
                   enable_ordering,
                   ack_deadline)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _format=self.format
        _project_id=self.project_id
        _topic=self.topic
        _authentication=self.authentication
        _seek_type=self.seek_type
        _seek_timestamp=(
            self.seek_timestamp
            if hasattr(self, "seek_timestamp")
            else None
        )
        _filter=(
            self.filter
            if hasattr(self, "filter")
            else None
        )
        _enable_ordering=(
            self.enable_ordering
            if hasattr(self, "enable_ordering")
            else None
        )
        _ack_deadline=(
            self.ack_deadline
            if hasattr(self, "ack_deadline")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"format={_format!r}, "
            f"project_id={_project_id!r}, "
            f"topic={_topic!r}, "
            f"authentication={_authentication!r}, "
            f"seek_type={_seek_type!r}, "
            f"seek_timestamp={_seek_timestamp!r}, "
            f"filter={_filter!r}, "
            f"enable_ordering={_enable_ordering!r}, "
            f"ack_deadline={_ack_deadline!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _format=self.format
        _project_id=self.project_id
        _topic=self.topic
        _authentication=self.authentication
        _seek_type=self.seek_type
        _seek_timestamp=(
            self.seek_timestamp
            if hasattr(self, "seek_timestamp")
            else None
        )
        _filter=(
            self.filter
            if hasattr(self, "filter")
            else None
        )
        _enable_ordering=(
            self.enable_ordering
            if hasattr(self, "enable_ordering")
            else None
        )
        _ack_deadline=(
            self.ack_deadline
            if hasattr(self, "ack_deadline")
            else None
        )
        return (
            f"{self.__class__.__name__}("
            f"format={_format!s}, "
            f"project_id={_project_id!s}, "
            f"topic={_topic!s}, "
            f"authentication={_authentication!s}, "
            f"seek_type={_seek_type!s}, "
            f"seek_timestamp={_seek_timestamp!s}, "
            f"filter={_filter!s}, "
            f"enable_ordering={_enable_ordering!s}, "
            f"ack_deadline={_ack_deadline!s}, "
            f")"
        )
