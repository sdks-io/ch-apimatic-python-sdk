"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.assigned_role import (
    AssignedRole,
)


class Invitation(object):
    """Implementation of the 'Invitation' model.

    Attributes:
        role (Role2): DEPRECATED. Use `assignedRoles` instead. Role of the invited
            user in the organization. For organizations that have migrated to custom
            roles, this field is frozen at the pre-migration value and does not
            reflect the role assignment that will be applied.
        id (uuid|str): Unique invitation ID.
        email (str): Email of the invited user. Only a user with this email can join
            using the invitation. The email is stored in a lowercase form.
        created_at (datetime): Invitation creation timestamp. ISO-8601.
        expire_at (datetime): Timestamp the invitation expires. ISO-8601.
        assigned_roles (List[AssignedRole]): Custom roles and System roles that will
            be assigned to the user when they accept the invitation
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "role": "role",
        "id": "id",
        "email": "email",
        "created_at": "createdAt",
        "expire_at": "expireAt",
        "assigned_roles": "assignedRoles",
    }

    _optionals = [
        "role",
        "id",
        "email",
        "created_at",
        "expire_at",
        "assigned_roles",
    ]

    def __init__(
        self,
        role=APIHelper.SKIP,
        id=APIHelper.SKIP,
        email=APIHelper.SKIP,
        created_at=APIHelper.SKIP,
        expire_at=APIHelper.SKIP,
        assigned_roles=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a Invitation instance."""
        # Initialize members of the class
        if role is not APIHelper.SKIP:
            self.role = role
        if id is not APIHelper.SKIP:
            self.id = id
        if email is not APIHelper.SKIP:
            self.email = email
        if created_at is not APIHelper.SKIP:
            self.created_at =\
                 APIHelper.apply_datetime_converter(
                created_at, APIHelper.RFC3339DateTime)\
                 if created_at else None
        if expire_at is not APIHelper.SKIP:
            self.expire_at =\
                 APIHelper.apply_datetime_converter(
                expire_at, APIHelper.RFC3339DateTime)\
                 if expire_at else None
        if assigned_roles is not APIHelper.SKIP:
            self.assigned_roles = assigned_roles

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        role =\
            dictionary.get("role")\
            if dictionary.get("role")\
                else APIHelper.SKIP
        id =\
            dictionary.get("id")\
            if dictionary.get("id")\
                else APIHelper.SKIP
        email =\
            dictionary.get("email")\
            if dictionary.get("email")\
                else APIHelper.SKIP
        created_at = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("createdAt")).datetime\
            if dictionary.get("createdAt") else APIHelper.SKIP
        expire_at = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("expireAt")).datetime\
            if dictionary.get("expireAt") else APIHelper.SKIP
        assigned_roles = None
        if dictionary.get("assignedRoles") is not None:
            assigned_roles = [
                AssignedRole.from_dictionary(x)
                    for x in dictionary.get("assignedRoles")
            ]
        else:
            assigned_roles = APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(role,
                   id,
                   email,
                   created_at,
                   expire_at,
                   assigned_roles,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _role=(
            self.role
            if hasattr(self, "role")
            else None
        )
        _id=(
            self.id
            if hasattr(self, "id")
            else None
        )
        _email=(
            self.email
            if hasattr(self, "email")
            else None
        )
        _created_at=(
            self.created_at
            if hasattr(self, "created_at")
            else None
        )
        _expire_at=(
            self.expire_at
            if hasattr(self, "expire_at")
            else None
        )
        _assigned_roles=(
            self.assigned_roles
            if hasattr(self, "assigned_roles")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"role={_role!r}, "
            f"id={_id!r}, "
            f"email={_email!r}, "
            f"created_at={_created_at!r}, "
            f"expire_at={_expire_at!r}, "
            f"assigned_roles={_assigned_roles!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _role=(
            self.role
            if hasattr(self, "role")
            else None
        )
        _id=(
            self.id
            if hasattr(self, "id")
            else None
        )
        _email=(
            self.email
            if hasattr(self, "email")
            else None
        )
        _created_at=(
            self.created_at
            if hasattr(self, "created_at")
            else None
        )
        _expire_at=(
            self.expire_at
            if hasattr(self, "expire_at")
            else None
        )
        _assigned_roles=(
            self.assigned_roles
            if hasattr(self, "assigned_roles")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"role={_role!s}, "
            f"id={_id!s}, "
            f"email={_email!s}, "
            f"created_at={_created_at!s}, "
            f"expire_at={_expire_at!s}, "
            f"assigned_roles={_assigned_roles!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
