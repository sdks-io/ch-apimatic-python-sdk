"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)


class ClickPipeScaling(object):
    """Implementation of the 'ClickPipeScaling' model.

    Attributes:
        replicas (int): Desired number of replicas. Only for scalable pipes.
        concurrency (int): Desired number of concurrency. Only for S3 pipes. If set
            to 0, concurrency is auto-scaled based on the cluster memory.
        replica_cpu_millicores (int): CPU in millicores for each replica. Only for
            streaming pipes.
        replica_memory_gb (float): Memory in GB for each replica. Only for streaming
            pipes.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "replicas": "replicas",
        "concurrency": "concurrency",
        "replica_cpu_millicores": "replicaCpuMillicores",
        "replica_memory_gb": "replicaMemoryGb",
    }

    _optionals = [
        "replicas",
        "concurrency",
        "replica_cpu_millicores",
        "replica_memory_gb",
    ]

    def __init__(
        self,
        replicas=APIHelper.SKIP,
        concurrency=APIHelper.SKIP,
        replica_cpu_millicores=APIHelper.SKIP,
        replica_memory_gb=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a ClickPipeScaling instance."""
        # Initialize members of the class
        if replicas is not APIHelper.SKIP:
            self.replicas = replicas
        if concurrency is not APIHelper.SKIP:
            self.concurrency = concurrency
        if replica_cpu_millicores is not APIHelper.SKIP:
            self.replica_cpu_millicores = replica_cpu_millicores
        if replica_memory_gb is not APIHelper.SKIP:
            self.replica_memory_gb = replica_memory_gb

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        replicas =\
            dictionary.get("replicas")\
            if dictionary.get("replicas")\
                else APIHelper.SKIP
        concurrency =\
            dictionary.get("concurrency")\
            if dictionary.get("concurrency")\
                else APIHelper.SKIP
        replica_cpu_millicores =\
            dictionary.get("replicaCpuMillicores")\
            if dictionary.get("replicaCpuMillicores")\
                else APIHelper.SKIP
        replica_memory_gb =\
            dictionary.get("replicaMemoryGb")\
            if dictionary.get("replicaMemoryGb")\
                else APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(replicas,
                   concurrency,
                   replica_cpu_millicores,
                   replica_memory_gb,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _replicas=(
            self.replicas
            if hasattr(self, "replicas")
            else None
        )
        _concurrency=(
            self.concurrency
            if hasattr(self, "concurrency")
            else None
        )
        _replica_cpu_millicores=(
            self.replica_cpu_millicores
            if hasattr(self, "replica_cpu_millicores")
            else None
        )
        _replica_memory_gb=(
            self.replica_memory_gb
            if hasattr(self, "replica_memory_gb")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"replicas={_replicas!r}, "
            f"concurrency={_concurrency!r}, "
            f"replica_cpu_millicores={_replica_cpu_millicores!r}, "
            f"replica_memory_gb={_replica_memory_gb!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _replicas=(
            self.replicas
            if hasattr(self, "replicas")
            else None
        )
        _concurrency=(
            self.concurrency
            if hasattr(self, "concurrency")
            else None
        )
        _replica_cpu_millicores=(
            self.replica_cpu_millicores
            if hasattr(self, "replica_cpu_millicores")
            else None
        )
        _replica_memory_gb=(
            self.replica_memory_gb
            if hasattr(self, "replica_memory_gb")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"replicas={_replicas!s}, "
            f"concurrency={_concurrency!s}, "
            f"replica_cpu_millicores={_replica_cpu_millicores!s}, "
            f"replica_memory_gb={_replica_memory_gb!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
