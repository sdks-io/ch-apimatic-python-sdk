"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: E501
from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.models.postgres_configuration import (
    PostgresConfiguration,
)
from openapispecforclickhousecloud.models.resource_tags_v_1 import (
    ResourceTagsV1,
)


class PostgresServiceRestoreRequest(object):
    """Implementation of the 'PostgresServiceRestoreRequest' model.

    Attributes:
        name (str): Name of the Postgres service. Alphanumerical string with
            whitespaces up to 50 characters.
        restore_target (datetime): The point in time at which to recover, as either
            date/time, named restore point, or a specific transaction ID.
        pg_config (PostgresConfiguration): Postgres [runtime
            configuration](https://www.postgresql.org/docs/current/runtime-config.html
            ) configuration.
        pg_bouncer_config (Dict[str, str]): PgBouncer [runtime
            configuration](https://www.pgbouncer.org/config.html) configuration.
        tags (List[ResourceTagsV1]): Tags associated with the Postgres service. Tag
            keys starting with “chc_” are reserved for internal use.
        additional_properties (Dict[str, Any]): The additional properties for the
            model.

    """

    # Create a mapping from Model property names to API property names
    _names = {
        "name": "name",
        "restore_target": "restoreTarget",
        "pg_config": "pgConfig",
        "pg_bouncer_config": "pgBouncerConfig",
        "tags": "tags",
    }

    _optionals = [
        "pg_config",
        "pg_bouncer_config",
        "tags",
    ]

    def __init__(
        self,
        name=None,
        restore_target=None,
        pg_config=APIHelper.SKIP,
        pg_bouncer_config=APIHelper.SKIP,
        tags=APIHelper.SKIP,
        additional_properties=None):
        """Initialize a PostgresServiceRestoreRequest instance."""
        # Initialize members of the class
        self.name = name
        self.restore_target =\
             APIHelper.apply_datetime_converter(
            restore_target, APIHelper.RFC3339DateTime)\
             if restore_target else None
        if pg_config is not APIHelper.SKIP:
            self.pg_config = pg_config
        if pg_bouncer_config is not APIHelper.SKIP:
            self.pg_bouncer_config = pg_bouncer_config
        if tags is not APIHelper.SKIP:
            self.tags = tags

        # Add additional model properties to the instance
        if additional_properties is None:
            additional_properties = {}
        self.additional_properties = additional_properties

    @classmethod
    def from_dictionary(cls,
                        dictionary):
        """Create an instance of this model from a dictionary

        Args:
            dictionary (dictionary): A dictionary representation of the object
            as obtained from the deserialization of the server's response. The
            keys MUST match property names in the API description.

        Returns:
            object: An instance of this structure class.

        """
        if not isinstance(dictionary, dict) or dictionary is None:
            return None

        # Extract variables from the dictionary
        name =\
            dictionary.get("name")\
            if dictionary.get("name")\
                else None
        restore_target = APIHelper.RFC3339DateTime.from_value(
            dictionary.get("restoreTarget")).datetime\
            if dictionary.get("restoreTarget") else None
        pg_config =\
            PostgresConfiguration.from_dictionary(
                dictionary.get("pgConfig"))\
                if "pgConfig" in dictionary.keys()\
                else APIHelper.SKIP
        pg_bouncer_config =\
            dictionary.get("pgBouncerConfig")\
            if dictionary.get("pgBouncerConfig")\
                else APIHelper.SKIP
        tags = None
        if dictionary.get("tags") is not None:
            tags = [
                ResourceTagsV1.from_dictionary(x)
                    for x in dictionary.get("tags")
            ]
        else:
            tags = APIHelper.SKIP

        additional_properties = APIHelper.get_additional_properties(
            dictionary={k: v for k, v in dictionary.items()
                        if k not in cls._names.values()},
            unboxing_function=lambda value: value)

        # Return an object of this model
        return cls(name,
                   restore_target,
                   pg_config,
                   pg_bouncer_config,
                   tags,
                   additional_properties)

    def __repr__(self):
        """Return a unambiguous string representation."""
        _name=self.name
        _restore_target=self.restore_target
        _pg_config=(
            self.pg_config
            if hasattr(self, "pg_config")
            else None
        )
        _pg_bouncer_config=(
            self.pg_bouncer_config
            if hasattr(self, "pg_bouncer_config")
            else None
        )
        _tags=(
            self.tags
            if hasattr(self, "tags")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"name={_name!r}, "
            f"restore_target={_restore_target!r}, "
            f"pg_config={_pg_config!r}, "
            f"pg_bouncer_config={_pg_bouncer_config!r}, "
            f"tags={_tags!r}, "
            f"additional_properties={_additional_properties!r}, "
            f")"
        )

    def __str__(self):
        """Return a human-readable string representation."""
        _name=self.name
        _restore_target=self.restore_target
        _pg_config=(
            self.pg_config
            if hasattr(self, "pg_config")
            else None
        )
        _pg_bouncer_config=(
            self.pg_bouncer_config
            if hasattr(self, "pg_bouncer_config")
            else None
        )
        _tags=(
            self.tags
            if hasattr(self, "tags")
            else None
        )
        _additional_properties=self.additional_properties
        return (
            f"{self.__class__.__name__}("
            f"name={_name!s}, "
            f"restore_target={_restore_target!s}, "
            f"pg_config={_pg_config!s}, "
            f"pg_bouncer_config={_pg_bouncer_config!s}, "
            f"tags={_tags!s}, "
            f"additional_properties={_additional_properties!s}, "
            f")"
        )
