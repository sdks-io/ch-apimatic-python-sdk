"""openapispecforclickhousecloud.

This file was automatically generated by APIMATIC v3.0 ( https://www.apimatic.io ).
"""

# ruff: noqa: D410, E501, E101, D206
from apimatic_core.authentication.multiple.single_auth import (
    Single,
)
from apimatic_core.request_builder import RequestBuilder
from apimatic_core.response_handler import ResponseHandler
from apimatic_core.types.parameter import Parameter

from openapispecforclickhousecloud.api_helper import (
    APIHelper,
)
from openapispecforclickhousecloud.apis.base_api import (
    BaseApi,
)
from openapispecforclickhousecloud.configuration import (
    Server,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgres400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgres500ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_ca_certificates_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgresCaCertificates400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_ca_certificates_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgresCaCertificates500ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_config_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgresConfig400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_config_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgresConfig500ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_logs_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgresLogs400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_logs_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgresLogs500ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_metrics_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgresMetrics400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_metrics_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgresMetrics500ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_password_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgresPassword400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_password_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgresPassword500ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_read_replica_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgresReadReplica400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_read_replica_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgresReadReplica500ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_restored_service_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgresRestoredService400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_restored_service_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgresRestoredService500ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_slow_query_patterns_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgresSlowQueryPatterns400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_slow_query_patterns_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgresSlowQueryPatterns500ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_slow_query_patterns_query_id_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgresSlowQueryPatternsQueryId400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_slow_query_patterns_query_id_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgresSlowQueryPatternsQueryId500ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_state_400_error_exception import (  # noqa: E501
    V1OrganizationsPostgresState400ErrorException,
)
from openapispecforclickhousecloud.exceptions.v_1_organizations_postgres_state_500_error_exception import (  # noqa: E501
    V1OrganizationsPostgresState500ErrorException,
)
from openapispecforclickhousecloud.http.http_method_enum import (
    HttpMethodEnum,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_config_response import (  # noqa: E501
    V1OrganizationsPostgresConfigResponse,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_config_response_1 import (  # noqa: E501
    V1OrganizationsPostgresConfigResponse1,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_logs_response import (  # noqa: E501
    V1OrganizationsPostgresLogsResponse,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_metrics_response import (  # noqa: E501
    V1OrganizationsPostgresMetricsResponse,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_password_response import (  # noqa: E501
    V1OrganizationsPostgresPasswordResponse,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_read_replica_response import (  # noqa: E501
    V1OrganizationsPostgresReadReplicaResponse,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_response import (
    V1OrganizationsPostgresResponse,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_response_1 import (
    V1OrganizationsPostgresResponse1,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_response_3 import (
    V1OrganizationsPostgresResponse3,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_restored_service_response import (  # noqa: E501
    V1OrganizationsPostgresRestoredServiceResponse,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_slow_query_patterns_query_id_response import (  # noqa: E501
    V1OrganizationsPostgresSlowQueryPatternsQueryIdResponse,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_slow_query_patterns_response import (  # noqa: E501
    V1OrganizationsPostgresSlowQueryPatternsResponse,
)
from openapispecforclickhousecloud.models.v_1_organizations_postgres_state_response import (  # noqa: E501
    V1OrganizationsPostgresStateResponse,
)


class PostgresApi(BaseApi):
    """A Controller to access Endpoints in the openapispecforclickhousecloud API."""

    def __init__(self, config):
        """Initialize PostgresApi object."""
        super(PostgresApi, self).__init__(config)

    def postgres_service_create(self,
                                organization_id,
                                body=None):
        """Perform a POST request to
        /v1/organizations/{organizationId}/postgres.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Creates a new Postgres service in
        the organization and returns it. The service is started asynchronously.

        Args:
            organization_id (uuid|str): ID of the organization that will own the
                service.
            body (PostgresServicePostRequest, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgres400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgres500ErrorException),
        ).execute()

    def postgres_service_get_list(self,
                                  organization_id):
        """Perform a GET request to
        /v1/organizations/{organizationId}/postgres.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Returns a list of all Postgres
        services in the organization.

        Args:
            organization_id (uuid|str): ID of the organization that owns the services.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresResponse1.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgres400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgres500ErrorException),
        ).execute()

    def postgres_service_get(self,
                             organization_id,
                             postgres_id):
        """Perform a GET request to
        /v1/organizations/{organizationId}/postgres/{postgresId}.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Returns a Postgres service that
        belongs to the organization

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgres400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgres500ErrorException),
        ).execute()

    def postgres_service_delete(self,
                                organization_id,
                                postgres_id):
        """Perform a DELETE request to
        /v1/organizations/{organizationId}/postgres/{postgresId}.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Deletes a Postgres service that
        belongs to the organization

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}")
            .http_method(HttpMethodEnum.DELETE)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresResponse3.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgres400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgres500ErrorException),
        ).execute()

    def postgres_service_patch(self,
                               organization_id,
                               postgres_id,
                               body=None):
        """Perform a PATCH request to
        /v1/organizations/{organizationId}/postgres/{postgresId}.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Update a Postgres service that
        belongs to the organization. **WARNING:** Changing the name also updates the
        host name and certificates for the service.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.
            body (PostgresServicePatchRequest, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}")
            .http_method(HttpMethodEnum.PATCH)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgres400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgres500ErrorException),
        ).execute()

    def postgres_service_certs_get(self,
                                   organization_id,
                                   postgres_id):
        """Perform a GET request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/caCertificates.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Download CA certificates for a
        PostgreSQL service

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/caCertificates")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.dynamic_deserialize)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresCaCertificates400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresCaCertificates500ErrorException),
        ).execute()

    def postgres_instance_restore(self,
                                  organization_id,
                                  postgres_id,
                                  body=None):
        """Perform a POST request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/restoredService.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Restore a Postgres database from
        continuous backup, optionally at a specific point in time.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.
            body (PostgresServiceRestoreRequest, optional): The request body
                parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/restoredService")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresRestoredServiceResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresRestoredService400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresRestoredService500ErrorException),
        ).execute()

    def postgres_service_set_password(self,
                                      organization_id,
                                      postgres_id,
                                      body=None):
        """Perform a PATCH request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/password.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Sets a new password for a Postgres
        service's superuser account.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.
            body (PostgresServiceSetPassword, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/password")
            .http_method(HttpMethodEnum.PATCH)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresPasswordResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresPassword400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresPassword500ErrorException),
        ).execute()

    def postgres_service_patch_state(self,
                                     organization_id,
                                     postgres_id,
                                     body=None):
        """Perform a PATCH request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/state.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Initiate a process for a Postgres
        service:
        * restart: Initiates a service restart
        * promote: Promotes a read replica to primary
        * switchover: Switch a primary over to a standby

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.
            body (PostgresServiceSetState, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/state")
            .http_method(HttpMethodEnum.PATCH)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresStateResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresState400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresState500ErrorException),
        ).execute()

    def postgres_instance_create_read_replica(self,
                                              organization_id,
                                              postgres_id,
                                              body=None):
        """Perform a POST request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/readReplica.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Initiate the process to create a new
        read replica for a Postgres service.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.
            body (PostgresServiceReadReplicaRequest, optional): The request body
                parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/readReplica")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresReadReplicaResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresReadReplica400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresReadReplica500ErrorException),
        ).execute()

    def postgres_instance_config_get(self,
                                     organization_id,
                                     postgres_id):
        """Perform a GET request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/config.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Returns the configuration data for a
        Postgres service and its PgBouncer service.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/config")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresConfigResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresConfig400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresConfig500ErrorException),
        ).execute()

    def postgres_instance_config_post(self,
                                      organization_id,
                                      postgres_id,
                                      body=None):
        """Perform a POST request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/config.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Replace the existing Postgres
        service and pgBouncer configuration.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.
            body (PostgresInstanceConfig, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/config")
            .http_method(HttpMethodEnum.POST)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresConfigResponse1.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresConfig400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresConfig500ErrorException),
        ).execute()

    def postgres_instance_config_patch(self,
                                       organization_id,
                                       postgres_id,
                                       body=None):
        """Perform a PATCH request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/config.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Update the existing Postgres service
        and pgBouncer configuration.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.
            body (PostgresInstanceConfig, optional): The request body parameter.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/config")
            .http_method(HttpMethodEnum.PATCH)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .header_param(Parameter()
                .key("Content-Type")
                .value("application/json"))
            .body_param(Parameter()
                .value(body))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .body_serializer(APIHelper.json_serialize)
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresConfigResponse1.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresConfig400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresConfig500ErrorException),
        ).execute()

    def postgres_instance_metrics_get(self,
                                      organization_id,
                                      postgres_id,
                                      from_date,
                                      to_date,
                                      bucket_size_seconds=None):
        """Perform a GET request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/metrics.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Returns bucketed time-series metrics
        for a PostgreSQL service over the requested window (CPU, memory, disk,
        network, connections, cache hit ratio, throughput, transactions, and more).
        Use this to chart or analyze how a service behaved over time.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the Postgres service.
            from_date (datetime): Inclusive start of the time window (RFC 3339
                date-time).
            to_date (datetime): Exclusive end of the time window (RFC 3339 date-time).
            bucket_size_seconds (int, optional): Time-series bucket size in seconds.
                When omitted, a bucket size is derived from the requested window.
                Requests are capped at 250 data points.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/metrics")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("from_date")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    from_date))
                .is_required(True))
            .query_param(Parameter()
                .key("to_date")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    to_date))
                .is_required(True))
            .query_param(Parameter()
                .key("bucket_size_seconds")
                .value(bucket_size_seconds))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresMetricsResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresMetrics400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresMetrics500ErrorException),
        ).execute()

    def slow_query_patterns_get_list(self,
                                     organization_id,
                                     postgres_id,
                                     from_date,
                                     to_date,
                                     db_name=None,
                                     db_user=None,
                                     db_operation=None,
                                     app=None,
                                     sort_by="total_duration",
                                     sort_order="desc",
                                     limit=20,
                                     offset=0):
        """Perform a GET request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/slowQueryPatterns.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Returns aggregate metrics for the
        slowest query patterns observed on a Postgres service during the given time
        window. Use this to discover which queries dominate total execution time,
        CPU, I/O, or WAL generation.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.
            from_date (datetime): Inclusive start of the time window (RFC 3339
                date-time).
            to_date (datetime): Exclusive end of the time window (RFC 3339 date-time).
            db_name (str, optional): Database name filter.
            db_user (str, optional): Database user filter.
            db_operation (str, optional): Database operation filter (for example,
                SELECT, INSERT, UPDATE, DELETE, UTILITY).
            app (str, optional): Application name filter.
            sort_by (SortBy, optional): Field to sort results by.
            sort_order (SortOrder1, optional): Sort order. One of `asc` or `desc`.
            limit (int, optional): Maximum number of results to return.
            offset (int, optional): Number of results to skip before returning.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/slowQueryPatterns")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("from_date")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    from_date))
                .is_required(True))
            .query_param(Parameter()
                .key("to_date")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    to_date))
                .is_required(True))
            .query_param(Parameter()
                .key("db_name")
                .value(db_name))
            .query_param(Parameter()
                .key("db_user")
                .value(db_user))
            .query_param(Parameter()
                .key("db_operation")
                .value(db_operation))
            .query_param(Parameter()
                .key("app")
                .value(app))
            .query_param(Parameter()
                .key("sort_by")
                .value(sort_by))
            .query_param(Parameter()
                .key("sort_order")
                .value(sort_order))
            .query_param(Parameter()
                .key("limit")
                .value(limit))
            .query_param(Parameter()
                .key("offset")
                .value(offset))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresSlowQueryPatternsResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresSlowQueryPatterns400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresSlowQueryPatterns500ErrorException),
        ).execute()

    def slow_query_pattern_get(self,
                               organization_id,
                               postgres_id,
                               query_id,
                               db_name,
                               db_user,
                               db_operation,
                               app=None,
                               timestamp=None):
        """Perform a GET request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/slowQueryPatterns/{que
        ryId}.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Returns aggregate metrics for a
        single slow query pattern together with its most recent individual executions.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.
            query_id (str): Stable identifier for the query pattern.
            db_name (str): Database name filter.
            db_user (str): Database user filter.
            db_operation (str): Database operation filter (for example, SELECT,
                INSERT, UPDATE, DELETE, UTILITY).
            app (str, optional): Application name filter.
            timestamp (datetime, optional): Timestamp of a specific execution (RFC
                3339).

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/slowQueryPatterns/{queryId}")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("queryId")
                .value(query_id)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("db_name")
                .value(db_name)
                .is_required(True))
            .query_param(Parameter()
                .key("db_user")
                .value(db_user)
                .is_required(True))
            .query_param(Parameter()
                .key("db_operation")
                .value(db_operation)
                .is_required(True))
            .query_param(Parameter()
                .key("app")
                .value(app))
            .query_param(Parameter()
                .key("timestamp")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    timestamp)))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresSlowQueryPatternsQueryIdResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresSlowQueryPatternsQueryId400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresSlowQueryPatternsQueryId500ErrorException),
        ).execute()

    def postgres_logs_get_list(self,
                               organization_id,
                               postgres_id,
                               from_date,
                               to_date,
                               body_contains=None,
                               severity=None,
                               sort_order="desc",
                               limit=50,
                               offset=0):
        """Perform a GET request to
        /v1/organizations/{organizationId}/postgres/{postgresId}/logs.

        **This endpoint is in beta.** API contract is stable, and no breaking changes
        are expected in the future. <br /><br /> Returns PostgreSQL server log
        entries for a Postgres service within the given time window, most recent
        first by default (override with `sort_order`). Results are paginated with
        `limit`/`offset`; advance `offset` until a page returns fewer than `limit`
        entries to read the full window. The time range must not exceed 30 days, and
        `to_date` must be after `from_date`.

        Args:
            organization_id (uuid|str): ID of the organization that owns the Postgres
                service.
            postgres_id (uuid|str): ID of the requested Postgres service.
            from_date (datetime): Inclusive start of the time window (RFC 3339
                date-time).
            to_date (datetime): Inclusive end of the time window (RFC 3339 date-time).
            body_contains (str, optional): Case-sensitive substring the log body must
                contain.
            severity (str, optional): Filter to log entries with this PostgreSQL
                severity (for example, ERROR, WARNING, LOG).
            sort_order (SortOrder1, optional): Sort order. One of `asc` or `desc`.
            limit (int, optional): Maximum number of results to return.
            offset (int, optional): Number of results to skip before returning.

        Returns:
            ApiResponse: An object with the response value as well as other useful
                information such as status codes and headers. Successful response

        Raises:
            ApiException: When an error occurs while fetching the data from the
                remote API. This exception includes the HTTP Response code, an error
                message, and the HTTP body that was received in the request.

        """
        return super().new_api_call_builder.request(
            RequestBuilder().server(Server.DEFAULT)
            .path("/v1/organizations/{organizationId}/postgres/{postgresId}/logs")
            .http_method(HttpMethodEnum.GET)
            .template_param(Parameter()
                .key("organizationId")
                .value(organization_id)
                .is_required(True)
                .should_encode(True))
            .template_param(Parameter()
                .key("postgresId")
                .value(postgres_id)
                .is_required(True)
                .should_encode(True))
            .query_param(Parameter()
                .key("from_date")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    from_date))
                .is_required(True))
            .query_param(Parameter()
                .key("to_date")
                .value(APIHelper.when_defined(APIHelper.RFC3339DateTime,
                    to_date))
                .is_required(True))
            .query_param(Parameter()
                .key("body_contains")
                .value(body_contains))
            .query_param(Parameter()
                .key("severity")
                .value(severity))
            .query_param(Parameter()
                .key("sort_order")
                .value(sort_order))
            .query_param(Parameter()
                .key("limit")
                .value(limit))
            .query_param(Parameter()
                .key("offset")
                .value(offset))
            .header_param(Parameter()
                .key("accept")
                .value("application/json"))
            .auth(Single("basicAuth")),
        ).response(
            ResponseHandler()
            .deserializer(APIHelper.json_deserialize)
            .deserialize_into(V1OrganizationsPostgresLogsResponse.from_dictionary)
            .is_api_response(True)
            .local_error("400",
                "The request cannot be processed due to a client error. Please verify"
                " your request parameters and try again.",
                V1OrganizationsPostgresLogs400ErrorException)
            .local_error("500",
                "An internal server error has occurred. If this issue persists, pleas"
                "e contact ClickHouse Cloud support for assistance.",
                V1OrganizationsPostgresLogs500ErrorException),
        ).execute()
